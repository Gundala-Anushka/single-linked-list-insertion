# Singly Linked List – Insertion Operations (C)

## 📌 Overview
This is a **menu-driven C program** for performing insertion operations in a singly linked list.  
It supports inserting a new node:
- At the **beginning** of the list
- At the **end** of the list
- At **any valid position** in the list

The program also includes a **Traverse** function to display all elements in the linked list.

---

## 🛠 Features
- **InsertAtBeginning**: Adds a new node at the start of the list.
- **InsertAtEnd**: Adds a new node at the end of the list.
- **InsertAtAny**: Inserts a new node at a specific position (if valid).
- **Traverse**: Displays all node values in order.
- **Menu-driven interface** for easy use.

---
## 📚 Concepts Covered
- Structures in C
- Dynamic memory allocation using malloc
- Singly linked list insertion
- Menu-driven programming
- Pointer manipulation
